import styled from '@emotion/styled';

export const Column = styled("div")({
  flex: "1 1 100%", 
  maxWidth: "100%",
  position: "relative",
  iframe: {
    maxWidth: "100%"
  }
})

export default Column;